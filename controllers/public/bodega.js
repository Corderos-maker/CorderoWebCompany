// ? método que se ejecuta al cargar la página
document.addEventListener("DOMContentLoaded", () => {
    // ? se carga el encabezado y pie de la página
    loadTemplate();
    MAIN_TITLE.innerText = 'SELECCIONE UNA CATEGORÍA';
});